{
  "scripts": {
  "backend": "node scripts/run-backend.js",
  "backend:reload": "node scripts/run-backend.js --reload",
  "frontend": "npm start --prefix frontend",
  "dev": "concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\""
},
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
