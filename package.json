{
  "scripts": {
  "backend": "python -m uvicorn src.api.app:app --host 127.0.0.1 --port 8000 --reload",
  "frontend": "npm start --prefix frontend",
  "dev": "concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\""
},
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/material": "^9.0.0",
    "@mui/x-chat": "^9.0.0-alpha.3"
  }
}
