## Why

<!-- The motivation, not the diff. What was wrong or missing for whom. -->

## What changed

<!-- One logical change. If you need a list, consider two PRs. -->

## Test plan

<!-- How a reviewer verifies it. Name the test that fails on the old behaviour. -->

## Documentation

Tick what this change touched, or say why none applies. The traceability
test and the changelog gate check some of this; the rest is on you.

- [ ] `docs/HOW_IT_WORKS.md` updated (any filter, prompt, source rule, estimator or review-flow change)
- [ ] Decision record added or superseded in `docs/decisions/` (any choice that would be expensive to relearn)
- [ ] `docs/LESSONS.md` entry added (this change came from a defect that cost time) with its guard test named
- [ ] `docs/CHANGELOG.md` entry (anything a reviewer or visitor would notice)
- [ ] `docs/OPERATIONS.md` updated (anything an operator runs or configures)
- [ ] None of the above applies because:

Commits are signed off (`git commit -s`); the org's DCO check requires it.
