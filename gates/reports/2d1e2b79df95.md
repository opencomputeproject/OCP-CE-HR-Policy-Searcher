<!-- proofmark-ship head=2d1e2b79df950d04365e95c8a01c8d84e8f4e2a8 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- 2d1e2b7 chore(deps): pin colord, js-yaml, svgo and qs to their patched versions

2 files changed, 21 insertions(+), 19 deletions(-)

## Test floor
2699 -> 2699  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 11251 1349 88%  measured 2026-09-08T21:47:46+00:00

## What the gates did
passes 1, bound 1, blocks 0, overrides 0, exceptions 0

## Rollback
    git revert --no-edit 55f2b9dbd24b..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: The six webpack-dev-server alerts (3, 4, 12, 24, 31, 32) remain; react-scripts 5 pins webpack-dev-server 4.x and only a CRA-to-Vite migration closes them. npm audit counts them as two moderates. The repo has no dependabot.yml, so Dependabot only opens PRs for advisories and every one of its PRs on this repo has failed npm ci; nobody has diagnosed why its generated lock differs from ours, and that is the real open question, not any single package.
- The diagnosis you got wrong first, and what corrected it: I treated this as four new packages to pin. The lock corrected it: js-yaml (both ranges) and svgo were already in the overrides block from #39 at versions one patch behind the new advisories, so three of the five edits are version bumps to existing entries and only colord and qs are new. Also, for the first time, `@dependabot recreate` was tried before superseding (the earlier PRs only had `rebase`); it produced a fresh commit that failed identically, which settles that the bot's lock generation, not its branch position, is the problem.
- Numbered open questions: 1. Why does Dependabot's regenerated lock fail npm ci when ours from the same package.json passes? (Likely the overrides block: Dependabot does not re-resolve overrides the way `npm install --package-lock-only` does.) 2. Should a dependabot.yml with `open-pull-requests-limit: 0` for npm be added so the bot stops opening PRs that can never pass, leaving the alerts tab as the signal? 3. Track the CRA-to-Vite migration as its own work package?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes. node_modules deleted and `npm ci` from the new lock installed 1,322 packages; `npm ls colord js-yaml svgo qs --all` listed colord@2.9.4, js-yaml@4.3.2 and js-yaml@3.15.2 (under @istanbuljs/load-nyc-config), svgo@2.8.4, qs@6.16.0, each marked overridden; a scan of package-lock.json found no other version of any of the four; `CI=true npx react-scripts test --watchAll=false` reported 547 passed in 42 suites; `npm audit` reported 2 moderate vulnerabilities, both webpack-dev-server.
