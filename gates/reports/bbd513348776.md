<!-- proofmark-ship head=bbd51334877661711f2757a3a04fa44200c809b8 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- bbd5133 docs(report): end-of-work report for wave 2
- 2a3ad32 feat(screening): a classifier call after the gate, evidence on every row
- 0121ea8 feat(review): read the reviewer's column into the app, switched off

100 files changed, 10054 insertions(+), 165 deletions(-)

## Test floor
2585 -> 2687  (ratcheted)

## Coverage (reported fact, never a gate)
TOTAL 10966 1340 88%  measured 2026-09-04T04:02:57+00:00

## What the gates did
passes 0, bound 0, blocks 0, overrides 3, exceptions 0
    override  post-commit: 0121ea8be624 landed without matching gate evidence (--no-verify, or the tree changed after the gate ran: TOCTO
    override  post-commit: 2a3ad32c8b6f landed without matching gate evidence (--no-verify, or the tree changed after the gate ran: TOCTO
    override  post-commit: bbd513348776 landed without matching gate evidence (--no-verify, or the tree changed after the gate ran: TOCTO
  NOTE: an override means a commit landed without matching gate evidence. Say so in the report rather than letting it sit in the ledger.

## Rollback
    git revert --no-edit 1b4ba6aa107f..HEAD
    # then restore gates/min_test_count.txt to 2585; the floor does not fall on its own

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: Six of the reviewer's 32 keeps have no usable screening fixture (four EU 'Have your say' pages, one regjeringen.no page and the archived White House order returned no text; three more are script shells under 300 characters), so the replay bar covers 23 keeps, not 32. The classifier's saving on Sonnet calls is unmeasured until the next monthly scan; the 1 September per-domain table is the number to compare. The kind lists are not yet on the admin Filters page (WP-7). Referenced-legislation matching for the duplicate fold is still deferred. Rebased onto the current main after the branch below it was squash-merged, so every commit id changed and this report replaces the one written for the pre-rebase commits; the findings are unchanged.
- The diagnosis you got wrong first, and what corrected it: Three times, each corrected by the recorded replay before anything shipped. First: a missing data-centre quote as a drop rule, which would have lost 12 of 23 keeps because Haiku failed to quote a sentence the regex had matched on 14 of them. Second: folding the yes/no into the three-question prompt, which lost 8. Third: the same with the original recall-first wording restored, which still lost 5. The fix that held was leaving the gate call untouched and making the classifier a second call, which lost none. A smaller one: I assumed the swallow baseline could be regenerated for a moved except block; the ratchet counts a renamed key as growth, and the right fix was narrowing the except to the API and connection errors it exists for.
- Numbered open questions: 1. ADR-0005: switch on output.import_reviews_before_scan and run the first import against production, with Keep as reviewed? 2. ADR-0007: set lane: signals on Kokkai? 3. Should the classifier's soft list gain plan or index after the next scan's evidence is read? 4. Is a second Haiku call per passed page (about $0.85 at the 1 September volume) an acceptable price for evidence on every row, or should the classifier run only for rows that reach the sheet? 5. Merge order with #38 and #40.
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes. 74 of the reviewer's decided rows were fetched through the project's own crawler and extractor (63 returned text; Playwright was needed for 21 of them) and their screening windows are committed under tests/fixtures/screening. The classifier prompt was sent once to Haiku for the 63 windows (130,403 input tokens, about $0.155) and the raw answers are committed; the replay test parses those answers and runs the gate: 0 of 23 usable keeps dropped, every removed report or article decided per rule. The three rejected designs were also recorded and replayed (about $0.34 more) before being discarded. The full fast suite passed at 2510 and the gate's own run at commit.
