<!-- proofmark-ship head=8111f59c44c628d65e2996d3be82f9f5755a3e2c -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- 8111f59 fix(storage): every store owns its connection and closes it

32 files changed, 408 insertions(+), 114 deletions(-)

## Test floor
2712 -> 2735  (ratcheted)

## Coverage (reported fact, never a gate)
TOTAL 11255 1345 88%  measured 2026-09-09T00:52:27+00:00

## What the gates did
passes 1, bound 1, blocks 1, overrides 0, exceptions 0
    block     vulture-orphan: unwired code (L003), not in baseline: ['src\\storage\\db.py::property::closed']

## Rollback
    git revert --no-edit 5bf12611a296..HEAD
    # then restore gates/min_test_count.txt to 2712; the floor does not fall on its own

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: connect() still runs the schema check and rebuilds the jurisdictions table from YAML on every call; with stores now long-lived that is once per process instead of per scan and per tool call, so it was left as-is rather than cached. Two concurrent scans now share one PolicyStore connection (check_same_thread=False, single asyncio thread) where each used to open its own; every write already committed immediately, so no behaviour changed, but it is a change in shape worth knowing. The remaining warnings under -W default (0 unclosed-database lines, from 3,139) come from tests that build stores inline; the harness closes those at teardown.
- The diagnosis you got wrong first, and what corrected it: I first wrote that every API request opened a new connection because deps.get_*_store() constructs a store; reading deps.py corrected it - every getter is @lru_cache, one store per process. The per-call leak was in the scanner, the agent tools, the digest tick, the mailer and the CLI entry points, not the request path. Second: I planned to wrap _run_scan's two stores in a with-block until I saw the function is 600 lines with three exit paths; making the ScanManager own the stores was smaller and also removed the per-scan jurisdictions rebuild.
- Numbered open questions: 1. Should connect() cache the schema-ensured, jurisdictions-rebuilt state per data_dir so a second store on the same db skips both? 2. Should the harness's leak-closing fixture instead FAIL a test that leaks a connection, now that every store can be closed, so tests model the ownership too? 3. The scanner's two scans now share one connection - should each scan get its own transaction scope instead?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes. 474 tests across the touched areas passed after the change; the new lifecycle tests build each of the ten store classes, close them, drop them and gc.collect() under warnings.catch_warnings, and record no ResourceWarning. The full suite with -W default: 0 unclosed-database lines (was 3,139 before this branch, counted the same way on 2026-09-08). ruff clean.

## Escapes to analyse - to be written by a person

One block per escape. A run of fix commits on one day for one thing is
one escape, not several. `none` is a real answer to the first question -
some defects no mechanical gate could have caught, and saying so is the
analysis. The second is yes or no.

### escape 2026-09-09 storage (1 fix commit)
- Analysis: no gate watches resource lifetime. The suite ran with ResourceWarnings hidden by default, so 3,139 of them were invisible until someone ran with -W default; nothing mechanical distinguishes a connection closed on purpose from one closed by the collector. The lifecycle test is the first check, and it is a test, not a gate.
- Which gate should have caught it (name one, or `none`): none
- Did that gate already exist and pass anyway (yes/no): no
