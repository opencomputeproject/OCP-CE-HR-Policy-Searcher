<!-- proofmark-ship head=87d8516dd468f11088aa186995528cac83f2cd30 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- 87d8516 docs(brief): the module-level small marker trap and the medium rules
- 7fb8971 test: size markers for 32 grandfathered files (batch 1 of 2)

34 files changed, 378 insertions(+), 1 deletion(-)

## Test floor
2712 -> 2712  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 11255 1345 88%  measured 2026-09-08T23:40:56+00:00

## What the gates did
passes 2, bound 2, blocks 0, overrides 0, exceptions 0

## Rollback
    git revert --no-edit c2f993a24773..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: 636 tests in 64 files are still unmarked; they are batch 2 (about 330 lines) and wait on this PR merging so the two do not stack. The gate's size guard takes the CLOSEST `small` marker, so a per-test `medium` inside a module-level-small file is still socket-blocked; that is a design property of gates/proofmark_sizes.py (ring-distributed, not edited here) and is documented in the brief rather than changed. Marking used static rules plus one run per file; a test that is hermetic today but reaches the network under some other fixture combination would not have been caught.
- The diagnosis you got wrong first, and what corrected it: The static pass marked test_api.py::TestConfigSettingsSecurity small because no test body named TestClient; the run corrected it with two SocketBlockedErrors - the class builds its client in a helper method. The rule now looks inside a class's helpers. Earlier: the batch file list I wrote had CRLF line endings, so the first apply crashed on 'test_keywords.py\r' before touching anything; trivial, but it is the kind of thing that looks like a tool bug for a minute.
- Numbered open questions: 1. Should the size guard (gates/proofmark_sizes.py) honour the closest marker of ANY size rather than the closest `small`, so a per-test medium can override a module-level small? That is a Proofmark change, not this repo's. 2. Once batch 2 lands and the baseline is 0, should the unmarked-ratchet's baseline file be removed so a new unmarked test blocks outright rather than ratcheting?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes. `pytest <the 32 files> -q -rf`: 1252 passed under the new markers (after the two TestConfigSettingsSecurity tests moved to medium; before that, 2 failed with pytest_socket.SocketBlockedError and 'ProactorEventLoop has no attribute _ssock', the Windows signature of a TestClient under small). `git diff --stat`: 32 files, 369 insertions, 0 deletions. The gate lowered gates/unmarked_baseline.txt 1678 -> 636 in the commit.
