<!-- proofmark-ship head=5f49a5e338484753fcf7bb5e639d5a825a078737 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- 5f49a5e docs(readme): two-audience section for visitors and administrators

1 file changed, 83 insertions(+), 10 deletions(-)

## Test floor
2735 -> 2735  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 11319 1333 88%  measured 2026-09-09T14:29:17+00:00

## What the gates did
passes 3, bound 1, blocks 1, overrides 0, exceptions 0
    block     ship-report: no report for 2 commit(s)

## Rollback
    git revert --no-edit 4b322ba99d0c..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: the live instance runs `public_review_visibility=default_all`, which the new section itself says is not the posture for a general audience. Left as is: a deploy-side setting, hers to change, not a README edit.
- The diagnosis you got wrong first, and what corrected it: I assumed the email notifications panel fired on scan completion. Reading `src/storage/notifications.py` showed the real topics are `early_signals` and `ops_alerts` at immediate/daily/weekly; the README line was rewritten to match before commit.
- Numbered open questions:
  1. Should the README name the Ascentra-hosted instance as the reference deployment for the OCP submission, or wait for an OCP-hosted one?
  2. The World Map section still points at `#key-features` for admin/reader mode; a link to the new administrators subsection would be shorter.
- Verified live by fetching real content (not a status code)? what, and what did it say: `GET https://policypulse.ascentrainnovations.com/health` returned `{"status":"ok","admin_required":true,"public_review_visibility":"default_all","config_version":1}`, confirming the token-gated admin claim and the posture named above. Panel names and order were read from `frontend/src/components/AgentPanel.js`, not from memory.
