<!-- proofmark-ship head=c19359cbd5c636fc5c867bcaf96cb0268b7ea1e2 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- c19359c chore(deps): pin browserslist 4.28.8 and postcss-selector-parser 6.1.4

2 files changed, 33 insertions(+), 28 deletions(-)

## Test floor
2689 -> 2689  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 11224 1351 88%  measured 2026-09-04T04:49:33+00:00

## What the gates did
passes 7, bound 1, blocks 3, overrides 0, exceptions 0
    block     ship-report: no report for 2 commit(s)
    block     ship-report: no report for 2 commit(s)
    block     ship-report: no report for 2 commit(s)

## Rollback
    git revert --no-edit 1853794bb63b..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: The six webpack-dev-server alerts inside Create React App remain; only a CRA-to-Vite migration closes those. npm audit could not be reached at the time of this commit (registry returned Service Unavailable), so the before/after audit counts are not recorded here; the Dependabot alert list on the repository is the authority.
- The diagnosis you got wrong first, and what corrected it: I assumed a `@dependabot rebase` comment would be enough to make PRs #43 and #44 mergeable. Both rebased onto the current main and both still failed npm ci with 'Missing: yaml@2.9.0 from lock file', while a clean npm ci from main's own lock succeeds locally. Rather than keep debugging a generated lock I do not control, the two bumps are pinned here as overrides beside the twelve from #39 and verified end to end.
- Numbered open questions: 1. Should the frontend CI job run `npm install --package-lock-only` and fail if the lock changes, so drift is caught on main rather than on every bot PR? 2. Track a CRA-to-Vite migration for the last six alerts?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes: node_modules removed, `npm ci` from this lock installed 1,322 packages, `npm ls` reports browserslist@4.28.8 and postcss-selector-parser@6.1.4 (with the nested 7.1.3 override intact), and the frontend suite passed 547 tests across 42 suites.
