<!-- proofmark-ship head=61cb4016b6403a2506386171f863e6eaeb1673a9 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- 61cb401 perf(core): read config through libyaml and retire datetime.utcnow()

14 files changed, 126 insertions(+), 28 deletions(-)

## Test floor
2690 -> 2694  (ratcheted)

## Coverage (reported fact, never a gate)
TOTAL 11224 1348 88%  measured 2026-09-08T19:53:37+00:00

## What the gates did
passes 1, bound 1, blocks 0, overrides 0, exceptions 0

## Rollback
    git revert --no-edit d70634fe83ee..HEAD
    # then restore gates/min_test_count.txt to 2690; the floor does not fall on its own

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: The eight other yaml.safe_load call sites in src/ (tools, extractor, jurisdictions, pricing, rules_version, mailer, eurlex_nim, intake) still use the pure-Python parser; each reads one small file, so the cost is milliseconds and they were left alone rather than touched for symmetry. The warnings that remain are essentially one defect, found while checking where they came from: ResourceWarning unclosed database in <sqlite3.Connection>, 3,139 of them under -W default. src/storage/db.py and the stores on it open SQLite connections and leave closing to garbage collection - a leak in production too. Not fixed here; it is its own change. crawler.py measures response_time_ms by subtracting two wall-clock readings; time.monotonic() would be the right tool, left as-is because the change here is the clock function, not the measurement.
- The diagnosis you got wrong first, and what corrected it: The #47 report blamed ConfigLoader itself and proposed a shared session-scoped fixture to avoid loading it fifteen times. cProfile corrected it: 1.49s of the 1.52s load was inside yaml.safe_load, i.e. PyYAML's pure-Python parser, and the C parser read the identical tree in 0.09s. Fixing the loader made the fixture idea unnecessary and also sped up the running app. Second: the new no-utcnow guard test failed on its first run because src/core/clock.py's own docstring names the call it replaces; the scan now exempts that one file.
- Numbered open questions: 1. Should the digest/mailer/schedule stores move to aware UTC datetimes in one deliberate migration (stored ISO strings would gain +00:00), or is the naive convention fine indefinitely now that it has one clock? 2. Should the crawler's elapsed-time measurements use time.monotonic()? 3. Should the suite run with -W error for DeprecationWarnings raised from src/ so the next deprecated call fails a commit instead of adding a warning?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes. cProfile on ConfigLoader.load(): 1.522s total, 1.494s in yaml.safe_load; CSafeLoader over every config/**/*.yaml: 0.09s. Full suite after: 2689 passed, 4 skipped, 1 xfailed in 124.61s (was 190.04s at #47), 1547 warnings (was 2443), slowest test 0.89s (was 1.60s). The equivalence test loaded all 98 config YAML files through both parsers and every dict compared equal.
