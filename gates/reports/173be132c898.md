<!-- proofmark-ship head=173be132c898b5c8f97385ae8f9b2116ba773420 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- 173be13 docs: production exports to the sheet of record, not the copy

4 files changed, 27 insertions(+), 14 deletions(-)

## Test floor
2735 -> 2735  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 11319 1333 88%  measured 2026-09-09T04:27:25+00:00

## What the gates did
passes 1, bound 1, blocks 0, overrides 0, exceptions 0

## Rollback
    git revert --no-edit 6f1016b3c06e..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: The switch ran under the interim service account (adoption checklist step 5, OCP's own account, is still open). The server's .env was rewritten with LF line endings where the backup has CRLF; compose reads both, but a diff of the two shows every line, not one. While verifying I ran the config loader from /tmp inside the container and it logged jurisdictions_registry_missing for a site-packages path - an artefact of running from outside /app, not an app defect, but worth knowing if it shows up again. The exporter's per-domain writes mean the 71 rows the copy gained reach her sheet only as their domains are rescanned; none were pushed today.
- The diagnosis you got wrong first, and what corrected it: None on the decision itself. Procedurally: the first two read-only checks silently did nothing because a python heredoc nested inside an ssh single-quoted command loses its body; copying a script file with scp and running it with `docker exec python /tmp/x.py` worked first time and is the shape to reuse. Also, the diff I printed to confirm the one-line .env change showed the whole file because the rewrite changed line endings - so the confirmation came from the grep and from the container's environment instead.
- Numbered open questions: 1. Should the 71 rows the copy holds and her sheet does not be pushed now in one export, so she sees them before 1 October rather than piecemeal as domains rescan? 2. Is the copy (1bMGQn...) now retired, or kept as a scratch target for tests? 3. With export and review on one sheet, does ADR-0005's import (still switched off) need review_spreadsheet_id at all?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes. Before: with production's credentials, the original opened as 'Heat Reuse Policies Database' with tabs [Heat Reuse Policies Database, Organizations/Authorities to contact, Staging], 29 headers, header AC 'Review (Is website trustworthy? ...)', 143 rows with a link; the copy 28 headers, 214 rows; 71 links only on the copy, 0 only on the original. After the switch, from inside the recreated container: ConfigLoader reports spreadsheet_id and review_spreadsheet_id both 1Az2Pz14eAGhre68BWmrpegg1PJ27decO0HRClKejJE8, and SheetsClient.get_existing_urls('Staging') returned 143. `docker exec policypulse env` shows the new SPREADSHEET_ID; container healthy 15 s after recreate.
