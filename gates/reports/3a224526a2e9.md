<!-- proofmark-ship head=3a224526a2e9ee77fd1a4249efbe39daf895f4ab -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- 3a22452 docs(report): ship report for the dependency bump commit
- 49f2641 chore(deps): patch 12 dependabot alerts via frontend overrides

3 files changed, 82 insertions(+), 27 deletions(-)

## Test floor
2394 -> 2394  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 11224 1351 88%  measured 2026-09-03T07:11:39+00:00

## What the gates did
passes 0, bound 0, blocks 0, overrides 2, exceptions 0
    override  post-commit: 49f2641c3ff5 landed without matching gate evidence (--no-verify, or the tree changed after the gate ran: TOCTO
    override  post-commit: 3a224526a2e9 landed without matching gate evidence (--no-verify, or the tree changed after the gate ran: TOCTO
  NOTE: an override means a commit landed without matching gate evidence. Say so in the report rather than letting it sit in the ledger.

## Rollback
    git revert --no-edit 0595959c3e8a..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: The six webpack-dev-server alerts inside Create React App remain (react-scripts 5 pins the 4.x line; only a CRA-to-Vite migration closes them). The top-level postcss-selector-parser 6.1.2 (low, alert 50) is left for Dependabot's own PR #36, which bumps it to 6.1.3 inside the ^6.x range the other plugins need. The stale report for the pre-rebase commit (gates/reports/ccaf66a0fe71.md) is removed in this commit because the commit it named no longer exists on this branch.
- The diagnosis you got wrong first, and what corrected it: None on the code. On process: the branch conflicted because Dependabot's #27 (brace-expansion ^1.1.16 to ^1.1.18) was merged to main after this branch was cut, and both touch the same override lines. Resolved by taking this branch's exact pins (1.1.18 and 2.1.4, which supersede #27's ranges), reconciling the lock with npm install --package-lock-only (which also added the yaml@2.9.0 entries whose absence made npm ci fail on every Dependabot PR), and proving it with a clean npm ci and the full frontend suite (531 of 531). The rebase replayed two commits without the pre-commit gate (the ledger records them as overrides); the pre-push full suite is the check that covers them.
- Numbered open questions: 1. Merge order: this PR first, then Dependabot's #36 and #43 once they rebase green. 2. Should the frontend CI job run npm install --package-lock-only as a drift check so a lock out of sync with package.json fails on main rather than on every Dependabot PR? 3. Track a CRA-to-Vite migration for the remaining six alerts?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes: npm ci from the reconciled lock installed 1,322 packages cleanly and the frontend suite passed 42 suites, 531 tests, on the rebased tree. The Dependabot failure on main was read from the failing job log: npm ci refusing a lock out of sync with package.json, missing yaml@2.9.0.
