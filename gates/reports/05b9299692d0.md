<!-- proofmark-ship head=05b9299692d0f7b0af8cda7069f0ebd482895320 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- 05b9299 test(screening): re-record the replay fixtures through the fixed extractor
- 3441d77 fix(extractor): never let a boilerplate match delete the page; pick the biggest main candidate

36 files changed, 3396 insertions(+), 323 deletions(-)

## Test floor
2702 -> 2712  (ratcheted)

## Coverage (reported fact, never a gate)
TOTAL 11251 1349 88%  measured 2026-09-08T23:04:22+00:00

## What the gates did
passes 2, bound 2, blocks 1, overrides 0, exceptions 0
    block     new-source-needs-tests: source gained code and no test changed: src/core/scanner.py (+2). Add or change a test under tests/ that fails

## Rollback
    git revert --no-edit 0ce906aae2fd..HEAD
    # then restore gates/min_test_count.txt to 2702; the floor does not fall on its own

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: regjeringen.no serves a Cloudflare challenge (HTTP 403, "Just a moment...") to httpx and to a headless browser alike; not worked around, by design, so r122 stays unusable and that document needs another source. The Have Your Say portal renders only the initiative summary block (588-3,008 chars), so those rows are usable but thin; the eu_have_your_say API source is the fuller route. config/content_extraction.yaml still contains the over-broad patterns (`sidebar`, `cookie`, `banner`) - the size guard makes them safe, the patterns themselves were not rewritten. The Google News fixture URLs are redirect links; the recorder follows them in the browser but the index keeps the redirect URL, not the article's.
- The diagnosis you got wrong first, and what corrected it: The chip (and I, on first read) treated nine unusable keeps as nine FETCH problems to solve per host. Probing each over httpx and Playwright corrected it: five of six hosts returned plenty of text (EUR-Lex 386,822 visible chars) and the extractor returned 0-191. Tracing the extractor stage by stage found two defects, not nine. Second: my recorded-page test asserted >1500 chars for the Have Your Say page and failed at 656; inspecting the pick showed 656 IS the whole rendered summary block, so the threshold was wrong, not the extractor. Third: after re-recording, the replay tests failed with 'no rows' - not lost keeps - because the `usable` flag had been hand-added to index.json in wave 2 and the recorder never wrote it.
- Numbered open questions: 1. Should the recorder store the final URL after redirects (Google News rows) so the fixture names the article actually screened? 2. Should `config/content_extraction.yaml` patterns be tightened (`sidebar` -> `^sidebar` / `-sidebar$`) now that the guard exists, or left as-is since the guard holds? 3. Is a document behind a Cloudflare challenge worth a manual PDF fixture, or is 'unreachable, documented' the right end state?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes, that was the whole method. Each host fetched live over httpx and Playwright, raw HTML saved and re-extracted: EUR-Lex 1 -> 381,144 chars; White House order 0 -> 81,117 (contains 'Sec. 2.  Policy'); emb3rs 49 -> 1,958; French article 92 -> 15,511; Have Your Say 0 -> 656/588/3,008/1,969 (text begins 'About this initiative / Summary / The energy efficiency first principle...'). Full re-record: 71 of 74 rows usable (was 58), 31 of 32 keeps (was 23), no row worse. record_screening_responses.py: 71 responses, 150,132 in / 5,687 out tokens, $0.179, prompt 70b35be83dbe. Replay: 88 tests passed incl. zero lost keeps over 31 keeps.

## Escapes to analyse - to be written by a person

One block per escape. A run of fix commits on one day for one thing is
one escape, not several. `none` is a real answer to the first question -
some defects no mechanical gate could have caught, and saying so is the
analysis. The second is yes or no.

### escape 2026-09-08 extractor (1 fix commit)
- Analysis: no gate measures extraction yield. The suite was green while the extractor returned 0 chars for whole classes of pages, because nothing asserted that a page with N visible characters yields text of the same order. The recorded-page tests and the replay's usable-count are the first mechanical checks of that, and they are tests, not gates.
- Which gate should have caught it (name one, or `none`): none
- Did that gate already exist and pass anyway (yes/no): no
