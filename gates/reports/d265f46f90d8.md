<!-- proofmark-ship head=d265f46f90d8a7e638878fa1d6a2cb34328d9c08 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- d265f46 fix(api): stop reading .env when src.api.app is imported

12 files changed, 202 insertions(+), 41 deletions(-)

## Test floor
2694 -> 2699  (ratcheted)

## Coverage (reported fact, never a gate)
TOTAL 11233 1348 88%  measured 2026-09-08T21:09:13+00:00

## What the gates did
passes 1, bound 1, blocks 1, overrides 0, exceptions 0
    block     unmarked-ratchet: 1679 unmarked tests > baseline 1678: a new test must carry a size marker - small (hermetic, enforced), medium 

## Rollback
    git revert --no-edit 68d178cebb6f..HEAD
    # then restore gates/min_test_count.txt to 2694; the floor does not fall on its own

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: src/api/app.py keeps two other import-time side effects - it defaults OCP_DATA_DIR and calls setup_logging(), which writes under data/logs on import. Neither reads secrets and the current tests rely on both, so they stay; moving them is its own change. src/api/routes/settings.py still calls load_dotenv(ENV_PATH, override=True) at request time after the admin saves a key - that is deliberate runtime behaviour, not an import side effect, and is untouched.
- The diagnosis you got wrong first, and what corrected it: I expected to drop override=True as the handoff suggested. test_env_loading.py corrected it: TestDotenvOverrideBehavior exists precisely because an empty stale shell variable must lose to the file, and test_all_use_override_true pins every entry point. So the runner keeps override=True and the tests now list src/api/__main__.py in place of app.py. I also first considered uvicorn's own --env-file flag, which loads without override and would have reopened that bug.
- Numbered open questions: 1. Should OCP_DATA_DIR defaulting and setup_logging also move out of import, so importing the module has no side effect at all? 2. Should the harness's load_dotenv neutraliser stay forever as a second lock, or be retired once the guard test has held for a while?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes. `python -m src.api --help` printed uvicorn's own usage text ("Usage: python -m src.api [OPTIONS] APP"), proving the runner reaches uvicorn's CLI. The new guard test was run against the OLD app.py by stashing the source change: 1 failed; with the change restored: 1 passed. 113 tests across the six touched files passed; the pre-commit gate ran the fast suite green.

## Escapes to analyse - to be written by a person

One block per escape. A run of fix commits on one day for one thing is
one escape, not several. `none` is a real answer to the first question -
some defects no mechanical gate could have caught, and saying so is the
analysis. The second is yes or no.

### escape 2026-09-08 api (1 fix commit)
- Analysis: the defect is a module mutating os.environ from a secrets file as a side effect of being imported. The size taxonomy's `small` enforces no-network via pytest-socket; nothing enforces no-environment-mutation, and no gate reads what an import does to the process. The closest mechanical check now is the new guard test (test_env_hermetic.py::test_importing_the_api_module_does_not_read_dotenv) - a test, not a gate, covering this one module.
- Which gate should have caught it (name one, or `none`): none
- Did that gate already exist and pass anyway (yes/no): no
