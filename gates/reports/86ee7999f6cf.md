<!-- proofmark-ship head=86ee7999f6cf4e40ddd257c186a3b79da5fd37ec -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- 86ee799 perf(tests): stop paying the clock, the network and the tracer at every push

11 files changed, 211 insertions(+), 11 deletions(-)

## Test floor
2689 -> 2690  (ratcheted)

## Coverage (reported fact, never a gate)
TOTAL 11224 1351 88%  measured 2026-09-04T05:23:45+00:00

## What the gates did
passes 1, bound 1, blocks 0, overrides 0, exceptions 0

## Rollback
    git revert --no-edit a3e5ef95b52d..HEAD
    # then restore gates/min_test_count.txt to 2689; the floor does not fall on its own

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: About fifteen tests spend roughly a second each in setup loading the real config/ tree through ConfigLoader (test_agent.py, tests/integration/test_full_pipeline.py); about 15s per run that a shared fixture would remove - separate change. The 2,400 DeprecationWarnings from datetime.utcnow() in src/ are untouched. The six webpack-dev-server and two qs Dependabot alerts are unrelated and untouched.
- The diagnosis you got wrong first, and what corrected it: The handoff (and my first read) blamed live network probes added by WP-3 for the 9-10 minute push. --durations=40 corrected it: the only live test ran in under a second; the time was 113s of real sleeps in two retry classes, 16s of real Chromium launches in three crawler tests that fetched example.gov over the internet, and a coverage tracer that doubled the whole run. Second wrong turn: a smoke test of the sysmon core reported 0% coverage and looked like a broken core; the same two files reported 0% under the default tracer too, because neither exercises src/ in-process. Comparing both cores on the full suite settled it (identical 11224/1348/88%).
- Numbered open questions: 1. Should the 1s ConfigLoader setups share a session-scoped fixture, or is a fresh loader per test worth 15s? 2. The live workflow runs Mondays; should a failure there open an issue automatically, or is the red badge enough? 3. Should the pre-commit fast run also set a hard per-test timeout so a future real sleep fails instead of quietly costing minutes?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes. The full suite ran three ways on this machine and the summary lines were read, not inferred: plain 2685 passed/4 skipped/1 xfailed in 190s; under the default coverage core 327s with TOTAL 11224 1348 88%; under sysmon 133s with the same TOTAL line. `pytest -m live --live` on test_sources_va_lis.py reached lis.blob.core.windows.net and passed (1 passed, 18 deselected). The before numbers came from the same commands on a clean worktree of main: 320s plain, 596s under coverage.
