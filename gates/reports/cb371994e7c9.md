<!-- proofmark-ship head=cb371994e7c9d9f5ca61f0ef02ae1bbdee0032b6 -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- cb37199 chore(ops): one monthly trigger - ADR-0006 accepted, cron line and workflow retired

6 files changed, 50 insertions(+), 138 deletions(-)

## Test floor
2735 -> 2735  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 11319 1333 88%  measured 2026-09-09T02:39:31+00:00

## What the gates did
passes 1, bound 1, blocks 0, overrides 0, exceptions 0

## Rollback
    git revert --no-edit 8495f0aa9b58..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: The static-explorer publish to GitHub Pages lived inside the deleted monthly workflow and had never run either; it is not recreated here. The weekly signals sweep still runs twice (server cron + the weekly-signals workflow against ci-data) by design per the runbook; that is a separate decision if the owner wants one trigger there too. The crontab's header comment on the server still says the POLICYPULSE jobs 'mirror .github/workflows schedules', which is now only true of the weekly line; a comment, left alone.
- The diagnosis you got wrong first, and what corrected it: None on this one; the ADR had already named the double-fire from 2 September's reading. What the deploy pre-flight added was the live confirmation: the row 81bcf50d still marked running eight days later, zero scan_domains rows, idle CPU. The one near-miss was procedural - my first attempt to mark the row used a heredoc through ssh and silently did nothing; the one-line form worked and was verified by reading the row back.
- Numbered open questions: 1. Should the weekly signals sweep also have one trigger (server cron OR the workflow), or is the belt-and-braces copy against ci-data wanted? 2. Should the static explorer publish return as its own workflow, run from the live database rather than from a scan of its own? 3. Should the scans table gain a notes column, so a row marked failed by hand can say why in place?
- Verified live by fetching real content (not a status code)? what, and what did it say: Yes. On ascentra-02: `diff backup new` printed exactly the one monthly line; after `crontab new`, `crontab -l | wc -l` = 57 and `grep POLICYPULSE` shows only the weekly line at 24; the restic line count is unchanged at 1. `select count(*) from scans where status='running'` = 0; row 81bcf50d reads ('failed', started 2026-09-01T06:00:22, completed 2026-09-09T04:15:34). The schedule row reads next_run_at 2026-10-01T06:00:00, enabled 1. Locally the traceability and docs-count tests pass with the workflow file gone.
